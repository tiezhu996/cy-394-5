请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述
**项目类型：纯后端服务**
搭建一个运动数据统计 API 服务，为健身应用提供用户运动记录的 CRUD 接口、统计分析接口和排行榜接口。

### 功能需求

1. **用户运动记录管理**：创建、查询、更新、删除运动记录（运动类型：跑步/骑行/游泳/力量训练/瑜伽/跳绳、时长、距离、卡路里、心率数据），支持按日期范围查询。
2. **运动类型管理**：预置运动类型字典，每种运动类型的 MET 值（代谢当量）用于卡路里计算。
3. **每日/周/月统计**：按时间维度聚合运动数据，返回总时长、总距离、总卡路里、运动次数、各类型占比。
4. **个人记录（PR）查询**：查询用户各项运动的历史最佳成绩（最长距离、最快配速、最大重量等）。
5. **排行榜**：本周/本月运动时长排行、跑步距离排行、累计卡路里消耗排行，支持按好友圈筛选。
6. **运动目标设定与进度**：用户设置每周运动目标（次数/时长/卡路里），API 返回当前完成进度百分比。
7. **健康数据对接**：提供 Webhook 接口，接收外部设备（手环/手表）推送的运动数据。

### 技术要求

- 后端框架：Go + Gin
- ORM：GORM
- 数据库：PostgreSQL
- 认证：JWT
- API 文档：Swagger（swaggo/swag 自动生成）
- 代码规范：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、运动类型枚举、MET 值表）统一维护在 `backend/internal/constants/` 目录下；日志统一使用 zap，日志配置在 `backend/internal/utils/logger.go`；异常信息统一定义在 `backend/internal/constants/errors.go`，后端统一错误处理中间件捕获并返回标准格式响应。

## 代码存放规则

纯后端 API 服务，无前端代码。

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端目录结构建议：
```
backend/
├── internal/
│   ├── router/             # 路由定义
│   ├── handler/            # 请求处理器
│   │   ├── user.go
│   │   ├── record.go
│   │   ├── stats.go
│   │   └── rank.go
│   ├── service/            # 业务逻辑
│   ├── repository/         # 数据访问层
│   ├── model/              # 数据模型
│   ├── middleware/          # 中间件（认证、错误处理、日志）
│   ├── constants/          # 常量、错误码、枚举
│   ├── utils/              # 工具函数
│   └── config/             # 配置
├── cmd/
│   └── main.go
├── docs/                   # Swagger 文档（自动生成）
├── database/
│   └── init.sql
└── go.mod
```

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：
- 根目录 `docker-compose.yml`（编排后端 + 数据库，无前端容器）
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=fitnessapi`
- `backend/Dockerfile`：Go 后端多阶段构建
- `.env.example`

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: fitnessapi`
- 容器名带 `fitnessapi-` 前缀
- 数据库数据使用命名卷持久化
- 数据库配置 `healthcheck`，后端 `depends_on` 数据库健康
- 端口映射：
  - 后端：19409:8080
- 访问 Swagger 文档：http://localhost:19409/swagger/index.html

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose）、本地开发方式、API 文档地址（Swagger）、技术栈（表格展示）、项目目录结构（代码块展示）、主要 API 列表、环境变量说明、License。

要求：Docker 部署命令放在最前面；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   .env
   .DS_Store
   *.log
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
